import { Next } from "../../../framework/types/serverPackageType";
export declare const getJoinLink: (date: string, time: string, duration: string, next: Next, zoomService: any) => Promise<string | void>;
